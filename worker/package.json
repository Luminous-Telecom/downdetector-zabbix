{
  "name": "downdetector-health-check-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker — monitors the Downdetector scraper via D1 heartbeat",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
